# Environment files (contain secrets)
.env
.env.local
.env.production
# OS files
.DS_Store
Thumbs.db

# ALL shell scripts (deployment/fix scripts may contain secrets)
*.sh

# Database exports (may contain sensitive data)
db-export.sql
auth-schema.sql
storage-schema.sql

# On-premises config overrides
onprem-*.js

# SQL files with user data
create-user.sql
